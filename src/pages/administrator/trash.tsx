import AdminLayout from '@/layouts/admin';
import React from 'react';

interface Props {}

const TrashPage = (props: Props) => {
  return <AdminLayout>TrashPage</AdminLayout>;
};

export default TrashPage;
