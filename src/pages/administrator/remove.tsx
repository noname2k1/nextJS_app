import AdminLayout from '@/layouts/admin';
import React from 'react';

interface Props {}

const RemovePage = (props: Props) => {
  return <AdminLayout>Remove page</AdminLayout>;
};

export default RemovePage;
